// Реальный авторский контент модуля 2 «База Go и инженерный подход».
// Ключи — id уроков вида go-foundations-<topic>-<n>, формируются в courseCurriculum.js.

export const goFoundations = {

  // Тема 1 · Основа Go-программы
  "go-foundations-go-core-1": {
    summary: "Из чего состоит любая программа на Go: пакет, импорт и точка входа main.",
    objectives: [
      "Объяснить назначение package main и func main",
      "Понять, зачем нужен import",
      "Собрать и запустить программу из нескольких строк",
    ],
    blocks: [
      { type: "dialogue", speaker: "Рома", role: "тимлид Go-команды", text: "Не нужно запоминать весь Go. Нужно научиться находить нужный инструмент, писать маленькие функции и проверять их." },
      { type: "heading", level: 2, text: "Четыре строки, которые есть в каждой программе" },
      { type: "code", language: "go", code: "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"старт стажировки завершён\")\n}" },
      { type: "paragraph", text: "**package main** говорит компилятору: это исполняемая программа, а не библиотека для переиспользования. **import \"fmt\"** подключает пакет форматированного ввода-вывода из стандартной библиотеки — без него `Println` был бы недоступен. **func main()** — единственная функция, с которой Go начинает выполнение: она находится автоматически, вызывать её вручную не нужно." },
      { type: "callout", tone: "tip", title: "Пакет — это просто папка", text: "Всё, что лежит в одной папке с одинаковым `package`, — один пакет. Можно разложить код по нескольким файлам внутри package main, компилятор соберёт их в одно целое." },
    ],
  },

  "go-foundations-go-core-2": {
    summary: "Переменные, короткое объявление := и базовые типы: int, float64, string, bool.",
    objectives: [
      "Объявить переменную через var и через :=",
      "Назвать четыре базовых типа данных",
      "Понять, что Go — язык со статической типизацией",
    ],
    blocks: [
      { type: "heading", level: 2, text: "var и := — два способа сказать одно и то же" },
      { type: "code", language: "go", code: "var taskCount int = 3       // явно указан тип\nvar title = \"Купить молоко\" // тип выведен автоматически\ndone := false                // короткое объявление, работает только внутри функций" },
      { type: "paragraph", text: "`var` можно использовать где угодно, включая уровень пакета; `:=` — только внутри функций, зато короче и используется чаще всего в реальном коде. Тип у переменной, объявленной через `:=`, всё равно определяется на этапе компиляции — просто компилятор выводит его сам из значения справа." },
      { type: "list", items: [
        "**int** — целое число: 42, -7",
        "**float64** — дробное число: 3.14",
        "**string** — текст в двойных кавычках: \"Bit Tech\"",
        "**bool** — true или false",
      ] },
      { type: "callout", tone: "warning", title: "Go не прощает неиспользуемые переменные", text: "Если объявить переменную и ни разу её не использовать, программа не скомпилируется. Это защита от забытого кода, а не баг компилятора." },
    ],
  },

  "go-foundations-go-core-3": {
    summary: "Условная логика в Go: if/else без скобок вокруг условия и практичный switch.",
    objectives: [
      "Написать условие if/else по правилам синтаксиса Go",
      "Использовать switch без явного break",
      "Понять короткую форму if с предварительным присваиванием",
    ],
    blocks: [
      { type: "heading", level: 2, text: "if без круглых скобок, но с обязательными фигурными" },
      { type: "code", language: "go", code: "if taskCount == 0 {\n\tfmt.Println(\"список пуст\")\n} else if taskCount < 5 {\n\tfmt.Println(\"немного задач\")\n} else {\n\tfmt.Println(\"список большой\")\n}" },
      { type: "paragraph", text: "В отличие от C или Java, условие в Go не берут в скобки — `if (x == 0)` даже вызовет предупреждение линтера. А вот фигурные скобки обязательны всегда, даже для одной строки внутри блока — тело if из одной команды без `{}` попросту не скомпилируется." },
      { type: "code", language: "go", code: "if err := saveTask(task); err != nil {\n\tfmt.Println(\"не удалось сохранить:\", err)\n}" },
      { type: "paragraph", text: "Частый паттерн Go — объявить переменную прямо в условии (`err := saveTask(task)`), она будет видна только внутри блока if/else. Это держит область видимости переменной ошибки минимальной." },
      { type: "paragraph", text: "`switch` в Go не требует `break` после каждого case — переход к следующему case не происходит по умолчанию (в отличие от C и Java), поэтому забытый break не станет источником бага." },
      { type: "code", language: "go", code: "switch level {\ncase \"easy\":\n\tfmt.Println(\"лёгкая задача\")\ncase \"medium\", \"hard\":\n\tfmt.Println(\"нужно больше времени\")\ndefault:\n\tfmt.Println(\"неизвестный уровень\")\n}" },
    ],
  },

  "go-foundations-go-core-4": {
    summary: "Единственный вид цикла в Go — for — и три его формы, включая range по коллекциям.",
    objectives: [
      "Написать классический for с тремя частями",
      "Использовать for как аналог while",
      "Перебрать слайс через range",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Один ключевик for на все случаи жизни" },
      { type: "paragraph", text: "В Go нет отдельных `while` или `do-while` — всё выражается через `for` в разных формах." },
      { type: "code", language: "go", code: "// классическая форма — счётчик\nfor i := 0; i < 5; i++ {\n\tfmt.Println(i)\n}\n\n// форма while — только условие\ncount := 0\nfor count < 3 {\n\tcount++\n}\n\n// бесконечный цикл — условие опущено полностью\nfor {\n\tbreak // без break такой цикл никогда не завершится сам\n}" },
      { type: "paragraph", text: "Чаще всего в реальном коде используется `range` — перебор элементов слайса, массива, map или строки:" },
      { type: "code", language: "go", code: "tasks := []string{\"Купить молоко\", \"Сдать отчёт\"}\nfor index, title := range tasks {\n\tfmt.Println(index, title)\n}\n// 0 Купить молоко\n// 1 Сдать отчёт" },
      { type: "callout", tone: "tip", title: "Не нужен индекс — используй _", text: "Если индекс не важен, замени его на `_`: `for _, title := range tasks`. Так компилятор не будет ругаться на неиспользуемую переменную." },
    ],
  },

  "go-foundations-go-core-5": {
    summary: "Функции с несколькими возвращаемыми значениями и главная идиома Go — ошибка как обычное значение.",
    objectives: [
      "Написать функцию, возвращающую два значения",
      "Понять идиому result, err := doSomething()",
      "Отличить панику от обычной ошибки",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Функция может вернуть больше одного значения" },
      { type: "code", language: "go", code: "func divide(a, b int) (int, error) {\n\tif b == 0 {\n\t\treturn 0, fmt.Errorf(\"деление на ноль\")\n\t}\n\treturn a / b, nil\n}\n\nresult, err := divide(10, 0)\nif err != nil {\n\tfmt.Println(\"ошибка:\", err)\n\treturn\n}\nfmt.Println(\"результат:\", result)" },
      { type: "paragraph", text: "В Go нет `try/catch`. Вместо этого функция, которая может завершиться неудачей, честно указывает это в своей сигнатуре — последним возвращаемым значением типа `error`. Вызывающий код обязан явно проверить `err != nil`, иначе легко пропустить ошибку — но именно эта явность и есть главная идея: ошибка видна прямо в объявлении функции, а не прячется где-то в глубине стека вызовов." },
      { type: "callout", tone: "tip", title: "Golang-присказка", text: "«Errors are values» — ошибка в Go такое же полноценное значение, как число или строка: её можно сохранить, сравнить, передать дальше по цепочке функций." },
      { type: "quiz", question: "Как в Go принято сообщать об ошибке из функции?", options: ["Через исключение try/catch", "Через panic при любой ошибке", "*Возвращая значение типа error последним из результатов функции"], explanation: "Стандартный паттерн Go — result, err := someFunc(), и обязательная проверка err != nil сразу после вызова." },
    ],
  },

  // Тема 2 · Данные в Go
  "go-foundations-data-model-1": {
    summary: "Структуры — способ Go описывать «вещи» с несколькими полями, аналог класса без наследования.",
    objectives: [
      "Объявить struct с несколькими полями",
      "Создать значение структуры и обратиться к полю через точку",
      "Понять, зачем структуры вместо отдельных переменных",
    ],
    blocks: [
      { type: "heading", level: 2, text: "struct — когда одной переменной мало" },
      { type: "code", language: "go", code: "type Task struct {\n\tTitle string\n\tDone  bool\n\tLevel string\n}\n\ntask := Task{Title: \"Купить молоко\", Done: false, Level: \"easy\"}\nfmt.Println(task.Title) // Купить молоко\ntask.Done = true         // поле можно менять после создания" },
      { type: "paragraph", text: "Вместо трёх отдельных переменных `title`, `done`, `level`, которые легко случайно перепутать местами при передаче в функцию, структура объединяет связанные данные в одну именованную сущность. Заглавная буква в имени поля (`Title`, а не `title`) — не стиль, а правило видимости: такие поля видны за пределами пакета, об этом подробнее поговорим в уроке про методы." },
    ],
  },

  "go-foundations-data-model-2": {
    summary: "Слайсы — гибкие списки поверх массивов, и как устроен append изнутри.",
    objectives: [
      "Отличить слайс от массива фиксированной длины",
      "Добавить элемент в слайс через append",
      "Понять, почему append иногда нужно переприсваивать результат",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Слайс — это список переменной длины" },
      { type: "paragraph", text: "Массив в Go имеет фиксированный размер, зашитый в тип (`[5]int` — это ровно 5 элементов, не больше и не меньше), поэтому на практике почти всегда используют **слайс** — гибкую обёртку поверх массива, которая умеет расти." },
      { type: "code", language: "go", code: "tasks := []string{\"Купить молоко\"}\ntasks = append(tasks, \"Сдать отчёт\")\ntasks = append(tasks, \"Позвонить Роме\")\nfmt.Println(tasks)       // [Купить молоко Сдать отчёт Позвонить Роме]\nfmt.Println(len(tasks))  // 3" },
      { type: "callout", tone: "warning", title: "Почему нужно писать tasks = append(...)", text: "append может не поместить новый элемент в старую память и выделить новый, больший массив под капотом. Поэтому результат всегда нужно сохранять обратно в переменную — забытое присваивание `tasks = ` — частая ошибка новичков." },
    ],
  },

  "go-foundations-data-model-3": {
    summary: "map — структура «ключ-значение» для быстрого поиска, и идиома comma-ok для проверки наличия ключа.",
    objectives: [
      "Создать map и добавить в неё пары ключ-значение",
      "Проверить наличие ключа через идиому value, ok",
      "Понять, чем поиск в map отличается от перебора слайса",
    ],
    blocks: [
      { type: "heading", level: 2, text: "map — мгновенный поиск по ключу" },
      { type: "code", language: "go", code: "statusByID := map[string]string{\n\t\"task-1\": \"done\",\n\t\"task-2\": \"in_progress\",\n}\nstatusByID[\"task-3\"] = \"todo\"\n\nfmt.Println(statusByID[\"task-1\"]) // done" },
      { type: "paragraph", text: "Если бы задачи хранились слайсом, поиск по ID требовал бы перебора всех элементов. map хранит пары ключ-значение так, что поиск по ключу почти мгновенный независимо от размера коллекции." },
      { type: "paragraph", text: "Обращение к несуществующему ключу не паникует, а просто вернёт нулевое значение для типа — но так легко перепутать «ключа нет» и «значение пустое». Для этого есть идиома comma-ok:" },
      { type: "code", language: "go", code: "status, ok := statusByID[\"task-99\"]\nif !ok {\n\tfmt.Println(\"такой задачи нет в map\")\n} else {\n\tfmt.Println(\"статус:\", status)\n}" },
    ],
  },

  "go-foundations-data-model-4": {
    summary: "Указатели без страха: зачем они нужны в Go и когда без них не обойтись.",
    objectives: [
      "Объяснить, что хранит указатель",
      "Получить указатель через & и разыменовать через *",
      "Понять единственную практическую причину использовать указатель — изменение исходных данных",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Указатель — это просто адрес, а не магия" },
      { type: "paragraph", text: "Когда переменная передаётся в функцию обычным способом, функция получает **копию** значения — изменения внутри функции не повлияют на оригинал. Указатель передаёт вместо копии адрес в памяти, и функция может изменить исходные данные напрямую." },
      { type: "code", language: "go", code: "func markDone(task Task) {\n\ttask.Done = true // меняет только копию, оригинал не тронут\n}\n\nfunc markDonePtr(task *Task) {\n\ttask.Done = true // меняет данные по адресу — оригинал изменится\n}\n\nt := Task{Title: \"Купить молоко\"}\nmarkDonePtr(&t) // & получает адрес переменной t\nfmt.Println(t.Done) // true" },
      { type: "paragraph", text: "В отличие от C, в Go нет арифметики указателей — нельзя сдвинуть указатель на N байт вперёд, он может указывать только на конкретный существующий объект. Это убирает целый класс ошибок с выходом за границы памяти." },
    ],
  },

  "go-foundations-data-model-5": {
    summary: "Методы — функции, привязанные к типу, и выбор между value receiver и pointer receiver.",
    objectives: [
      "Объявить метод с получателем (receiver)",
      "Выбрать между value receiver и pointer receiver осознанно",
      "Понять, чем метод отличается от обычной функции",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Метод — это функция, которая знает, к какому типу относится" },
      { type: "code", language: "go", code: "type Task struct {\n\tTitle string\n\tDone  bool\n}\n\nfunc (t *Task) Complete() {\n\tt.Done = true\n}\n\ntask := Task{Title: \"Купить молоко\"}\ntask.Complete()\nfmt.Println(task.Done) // true" },
      { type: "paragraph", text: "`(t *Task)` перед именем метода — это **receiver**, он и делает функцию методом типа Task. Есть два вида receiver:" },
      { type: "list", items: [
        "**value receiver** `(t Task)` — метод получает копию, подходит для методов, которые только читают данные",
        "**pointer receiver** `(t *Task)` — метод получает адрес и может менять исходные данные, как в примере выше",
      ] },
      { type: "callout", tone: "tip", title: "Простое правило выбора", text: "Если метод должен изменить состояние объекта — используй pointer receiver. Если только читает данные и возвращает результат — value receiver вполне достаточно." },
      { type: "quiz", question: "Какой receiver нужен методу, который должен изменить поле структуры?", options: ["Любой — оба варианта одинаково изменят структуру", "*Pointer receiver, например (t *Task)", "Value receiver, например (t Task)"], explanation: "Value receiver работает с копией — изменения внутри метода не затронут оригинальный объект вне метода." },
    ],
  },

  // Тема 3 · Файлы, JSON и конфигурация
  "go-foundations-files-json-1": {
    summary: "Чтение и запись файлов через пакет os: os.ReadFile и os.WriteFile.",
    objectives: [
      "Прочитать содержимое файла в память",
      "Записать данные в файл",
      "Обработать ошибку отсутствующего файла",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Работа с файлами в две строчки" },
      { type: "code", language: "go", code: "data, err := os.ReadFile(\"tasks.json\")\nif err != nil {\n\tfmt.Println(\"не удалось прочитать файл:\", err)\n\treturn\n}\nfmt.Println(string(data))\n\nerr = os.WriteFile(\"tasks.json\", []byte(\"[]\"), 0644)\nif err != nil {\n\tfmt.Println(\"не удалось сохранить файл:\", err)\n}" },
      { type: "paragraph", text: "`os.ReadFile` возвращает содержимое файла в виде `[]byte` — «сырых» байтов, а не сразу строки, поэтому для вывода их приводят через `string(data)`. Число `0644` в `WriteFile` — права доступа к файлу в Unix-нотации: владелец может читать и писать, остальные — только читать." },
      { type: "callout", tone: "warning", title: "Файла может не быть — это нормальная ситуация", text: "При первом запуске Task Tracker файла tasks.json ещё не существует. Ошибку os.ReadFile в этом случае стоит не игнорировать, а обработать: начать с пустого списка задач." },
    ],
  },

  "go-foundations-files-json-2": {
    summary: "Превращаем Go-структуры в JSON и обратно через json.Marshal и json.Unmarshal.",
    objectives: [
      "Сериализовать структуру в JSON",
      "Разобрать JSON обратно в структуру",
      "Понять, зачем это нужно для сохранения списка задач",
    ],
    blocks: [
      { type: "heading", level: 2, text: "encoding/json — мост между Go и текстовым форматом" },
      { type: "code", language: "go", code: "type Task struct {\n\tTitle string\n\tDone  bool\n}\n\ntasks := []Task{{Title: \"Купить молоко\", Done: false}}\n\ndata, _ := json.Marshal(tasks)\nfmt.Println(string(data))\n// [{\"Title\":\"Купить молоко\",\"Done\":false}]\n\nvar loaded []Task\njson.Unmarshal(data, &loaded)\nfmt.Println(loaded[0].Title) // Купить молоко" },
      { type: "paragraph", text: "`json.Marshal` превращает Go-значение в срез байт с JSON-текстом — это то, что затем запишется в файл через `os.WriteFile`. `json.Unmarshal` делает обратное: разбирает JSON-текст обратно в структуру. Обрати внимание на `&loaded` — Unmarshal нужен указатель, чтобы заполнить переданную переменную напрямую, а не вернуть новую копию." },
    ],
  },

  "go-foundations-files-json-3": {
    summary: "Теги структур управляют тем, как поле выглядит в JSON — имя, пропуск пустых значений и не только.",
    objectives: [
      "Задать кастомное имя JSON-поля через тег",
      "Использовать omitempty",
      "Понять, зачем теги нужны, если у Go и JSON разные соглашения об именах",
    ],
    blocks: [
      { type: "heading", level: 2, text: "`json:\"title\"` — маленькая строка с большим эффектом" },
      { type: "paragraph", text: "В Go принято называть публичные поля с Заглавной буквы, а в JSON обычно используют snake_case или camelCase. Теги структур решают это несоответствие:" },
      { type: "code", language: "go", code: "type Task struct {\n\tTitle    string `json:\"title\"`\n\tDone     bool   `json:\"done\"`\n\tPriority int    `json:\"priority,omitempty\"`\n}" },
      { type: "paragraph", text: "Без тега поле `Title` попало бы в JSON как `\"Title\"` с заглавной буквы — не страшно, но не соответствует привычному стилю API. `omitempty` у Priority говорит: если значение нулевое (0 для int, \"\" для string, false для bool), не включать поле в JSON вовсе — удобно для необязательных полей." },
    ],
  },

  "go-foundations-files-json-4": {
    summary: "Что делать, когда JSON пришёл битым: защитная обработка ошибок разбора данных.",
    objectives: [
      "Обработать ошибку json.Unmarshal, не уронив программу",
      "Отличить отсутствующий файл от файла с некорректным содержимым",
      "Спроектировать разумное поведение по умолчанию при повреждённых данных",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Файл может существовать, но быть бесполезным" },
      { type: "paragraph", text: "Файл `tasks.json` может исчезнуть, стать пустым или получить повреждение (например, если процесс упал посреди записи). Хорошая программа не падает в этих случаях, а решает, что делать дальше." },
      { type: "code", language: "go", code: "func loadTasks(path string) ([]Task, error) {\n\tdata, err := os.ReadFile(path)\n\tif err != nil {\n\t\tif os.IsNotExist(err) {\n\t\t\treturn []Task{}, nil // файла ещё нет — это не ошибка, а старт с пустого списка\n\t\t}\n\t\treturn nil, fmt.Errorf(\"чтение файла: %w\", err)\n\t}\n\n\tvar tasks []Task\n\tif err := json.Unmarshal(data, &tasks); err != nil {\n\t\treturn nil, fmt.Errorf(\"файл повреждён, ожидался JSON: %w\", err)\n\t}\n\treturn tasks, nil\n}" },
      { type: "callout", tone: "tip", title: "%w — оборачивание ошибки", text: "fmt.Errorf с глаголом %w сохраняет исходную ошибку внутри новой — так вызывающий код видит и твоё пояснение, и техническую причину, а errors.Is/As всё ещё могут найти исходную ошибку." },
    ],
  },

  "go-foundations-files-json-5": {
    summary: "Читаем настройки из переменных окружения вместо того, чтобы зашивать их в код.",
    objectives: [
      "Прочитать переменную окружения через os.Getenv",
      "Задать значение по умолчанию, если переменная не установлена",
      "Собрать простую структуру конфигурации приложения",
    ],
    blocks: [
      { type: "heading", level: 2, text: "os.Getenv и значения по умолчанию" },
      { type: "code", language: "go", code: "storagePath := os.Getenv(\"STORAGE_PATH\")\nif storagePath == \"\" {\n\tstoragePath = \"tasks.json\" // разумное значение по умолчанию\n}\n\ntype Config struct {\n\tStoragePath string\n\tDebug       bool\n}\n\nfunc loadConfig() Config {\n\treturn Config{\n\t\tStoragePath: getEnvOrDefault(\"STORAGE_PATH\", \"tasks.json\"),\n\t\tDebug:       os.Getenv(\"DEBUG\") == \"true\",\n\t}\n}" },
      { type: "paragraph", text: "`os.Getenv` никогда не паникует — если переменной нет, возвращается пустая строка. Поэтому в коде принято сразу проверять на пустоту и подставлять разумное значение по умолчанию. Собирая все настройки в одну структуру `Config`, ты получаешь единое место, откуда остальной код читает конфигурацию — вместо разрозненных вызовов os.Getenv по всему проекту." },
      { type: "quiz", question: "Что вернёт os.Getenv(\"НЕСУЩЕСТВУЮЩАЯ_ПЕРЕМЕННАЯ\")?", options: ["Программа завершится с ошибкой", "nil", "*Пустую строку \"\""], explanation: "os.Getenv никогда не паникует и не возвращает ошибку — при отсутствии переменной результат просто пустая строка, это нужно проверять самостоятельно." },
    ],
  },

  // Тема 4 · Тесты и качество
  "go-foundations-quality-1": {
    summary: "Пишем первый unit-тест: файл _test.go, функция TestXxx и запуск через go test.",
    objectives: [
      "Создать файл с тестом по соглашению *_test.go",
      "Написать функцию TestXxx(t *testing.T)",
      "Запустить тесты командой go test",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Тесты живут рядом с кодом, а не отдельно" },
      { type: "paragraph", text: "Тест для файла `tasks.go` кладут в `tasks_test.go` — в том же пакете, но с суффиксом `_test`. Go находит такие файлы автоматически и не включает их в финальный бинарник." },
      { type: "code", language: "go", code: "// tasks.go\nfunc FormatTitle(title string) string {\n\treturn strings.TrimSpace(title)\n}\n\n// tasks_test.go\nfunc TestFormatTitle(t *testing.T) {\n\tresult := FormatTitle(\"  Купить молоко  \")\n\tif result != \"Купить молоко\" {\n\t\tt.Errorf(\"ожидали %q, получили %q\", \"Купить молоко\", result)\n\t}\n}" },
      { type: "code", language: "bash", code: "go test ./...\n# ok  github.com/anna-dev/task-tracker  0.240s" },
      { type: "paragraph", text: "Имя тестовой функции обязано начинаться с `Test`, дальше — заглавная буква (`TestFormatTitle`, не `Testformattitle`). `t.Errorf` не останавливает тест немедленно — он отмечает тест как проваленный и продолжает выполнение, что позволяет увидеть сразу несколько несовпадений за один запуск." },
    ],
  },

  "go-foundations-quality-2": {
    summary: "Табличные тесты — идиоматичный способ Go проверить много входных данных одним тестом.",
    objectives: [
      "Написать табличный тест со слайсом кейсов",
      "Использовать t.Run для подтестов с именами",
      "Понять преимущество перед копированием одного и того же теста",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Один тест, много сценариев" },
      { type: "paragraph", text: "Вместо того чтобы копировать один и тот же тест пять раз с разными входными данными, в Go принято собирать сценарии в слайс структур и перебирать его циклом — это называется **table-driven test**." },
      { type: "code", language: "go", code: "func TestFormatTitle(t *testing.T) {\n\tcases := []struct {\n\t\tname  string\n\t\tinput string\n\t\twant  string\n\t}{\n\t\t{name: \"обрезает пробелы\", input: \"  молоко  \", want: \"молоко\"},\n\t\t{name: \"пустая строка остаётся пустой\", input: \"\", want: \"\"},\n\t\t{name: \"без пробелов не меняется\", input: \"отчёт\", want: \"отчёт\"},\n\t}\n\n\tfor _, tc := range cases {\n\t\tt.Run(tc.name, func(t *testing.T) {\n\t\t\tif got := FormatTitle(tc.input); got != tc.want {\n\t\t\t\tt.Errorf(\"FormatTitle(%q) = %q, хотели %q\", tc.input, got, tc.want)\n\t\t\t}\n\t\t})\n\t}\n}" },
      { type: "callout", tone: "tip", title: "t.Run даёт именованные подтесты", text: "go test -v покажет каждый сценарий отдельной строкой с его именем — сразу видно, какой конкретно кейс упал, без чтения кода теста." },
    ],
  },

  "go-foundations-quality-3": {
    summary: "Граничные сценарии: пустой вход, нулевые значения и nil — где чаще всего прячутся баги.",
    objectives: [
      "Перечислить типичные граничные случаи для функции",
      "Написать тест на пустой слайс и на nil",
      "Понять, почему «счастливый путь» — это не вся проверка",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Баг живёт на границах, а не в середине" },
      { type: "paragraph", text: "Функция, которая корректно работает с обычными данными, часто ломается на краевых случаях — Игорь как раз ищет их в первую очередь при проверке проектов. Стандартный чек-лист граничных сценариев для функции над коллекцией:" },
      { type: "list", items: [
        "Пустой слайс или map — `[]Task{}`",
        "nil вместо инициализированной коллекции",
        "Один-единственный элемент",
        "Очень длинная строка или очень большое число",
        "Отрицательное число там, где ожидается количество",
      ] },
      { type: "code", language: "go", code: "func TestAverage_EmptySlice(t *testing.T) {\n\tresult := Average([]int{})\n\tif result != 0 {\n\t\tt.Errorf(\"среднее пустого списка должно быть 0, получили %v\", result)\n\t}\n}" },
      { type: "callout", tone: "warning", title: "nil-слайс — не то же самое, что паника", text: "В Go цикл range по nil-слайсу отработает нормально — просто ноль итераций. А вот обращение к nil-указателю по полю вызовет панику. Это разные ситуации, их стоит различать в голове." },
    ],
  },

  "go-foundations-quality-4": {
    summary: "Инструменты, которые проверяют код за тебя: gofmt для стиля и go vet для подозрительных мест.",
    objectives: [
      "Отформатировать код командой gofmt",
      "Найти потенциальную ошибку через go vet",
      "Понять разницу между стилем и логической проблемой",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Два разных вопроса: «красиво ли» и «нет ли здесь ошибки»" },
      { type: "paragraph", text: "**gofmt** не спорит о стиле — он приводит любой код к единственному каноническому виду (отступы, пробелы, выравнивание). Если настроить «Format On Save» (модуль 1, тема про VS Code), запускать его вручную почти не придётся." },
      { type: "code", language: "bash", code: "gofmt -l .          # список файлов, которые не отформатированы\ngofmt -w main.go     # переписать файл в правильном формате" },
      { type: "paragraph", text: "**go vet** работает на другом уровне — ищет код, который синтаксически корректен, но скорее всего содержит ошибку: неправильную строку формата в `Printf`, забытый `%s` для строки там, где передано число, недостижимый код после `return`." },
      { type: "code", language: "bash", code: "go vet ./...\n# ./main.go:12:2: fmt.Println call has possible formatting directive %s" },
      { type: "callout", tone: "tip", title: "Оба — часть go test за кулисами", text: "go test автоматически запускает часть проверок go vet перед тестами, так что явные ошибки формата вылезут наружу, даже если ты забыл вызвать go vet руками." },
    ],
  },

  "go-foundations-quality-5": {
    summary: "Как отлаживать Go-код: осмысленные сообщения об ошибках и простые приёмы вместо debugger с первого дня.",
    objectives: [
      "Написать информативное сообщение об ошибке",
      "Использовать fmt.Printf для отладочного вывода",
      "Понять, когда стоит перейти от print к настоящему debugger",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Хорошая ошибка отвечает на вопрос «что теперь делать»" },
      { type: "code", language: "go", code: "// плохо — непонятно, что произошло и с чем\nreturn errors.New(\"error\")\n\n// лучше — понятно, что искать\nreturn fmt.Errorf(\"не удалось сохранить задачу %q: %w\", task.Title, err)" },
      { type: "paragraph", text: "Сообщение об ошибке читает не только компьютер, но и человек — в том числе ты сам через неделю. Хорошее сообщение называет, ЧТО не получилось и по возможности С КАКИМИ данными, а не просто «error» или «something went wrong»." },
      { type: "paragraph", text: "Для быстрой отладки на старте достаточно `fmt.Println` или `fmt.Printf` в подозрительных местах кода — временный вывод значения переменной часто быстрее, чем настройка полноценного debugger. Когда логика усложнится (несколько горутин, состояние меняется в разных местах), пригодится встроенный в VS Code debugger — но не раньше, чем print перестанет справляться." },
      { type: "callout", tone: "tip", title: "Не забывай убирать отладочный вывод", text: "fmt.Println(\"здесь\", x) отлично помогает в моменте, но легко забыть его в коммите. Перед git add лишний раз проверь git diff — привычка из модуля 1 пригождается здесь." },
      { type: "quiz", question: "Чем плохо сообщение об ошибке вида errors.New(\"error\")?", options: ["Такой код не скомпилируется", "*Оно не объясняет, что именно и с какими данными пошло не так", "errors.New нельзя использовать в Go"], explanation: "Сообщение должно быть информативным для человека, который будет разбираться с проблемой — «error» не даёт вообще никакого контекста." },
    ],
  },

  // Тема 5 · Основы PostgreSQL
  "go-foundations-database-basics-1": {
    summary: "Что такое реляционные данные и почему второй проект стажировки перерастает JSON-файл.",
    objectives: [
      "Объяснить, чем таблица отличается от JSON-файла со списком",
      "Понять понятия строки (row) и столбца (column)",
      "Увидеть, где JSON-подход из первого проекта начинает буксовать",
    ],
    blocks: [
      { type: "dialogue", speaker: "Рома", role: "тимлид Go-команды", text: "Task Tracker прекрасно жил с одним JSON-файлом. Но как только данных становится много и они начинают ссылаться друг на друга — нужна база данных." },
      { type: "heading", level: 2, text: "Таблица — это структурированный список с правилами" },
      { type: "paragraph", text: "**Реляционная база данных** хранит данные в таблицах: у таблицы фиксированный набор столбцов (например, `id`, `title`, `amount`, `category`), а каждая строка — это одна запись, как одна задача в Task Tracker. В отличие от JSON-файла, база данных умеет проверять целостность данных на своей стороне (например, не даст сохранить расход без суммы) и быстро искать по любому полю, даже когда записей миллионы." },
      { type: "paragraph", text: "Ключевая идея слова «реляционная» — таблицы могут **ссылаться** друг на друга. Во втором проекте, Expense Tracker, у каждого расхода будет категория — вместо того чтобы дублировать название категории в каждой записи, на неё будут ссылаться по идентификатору из отдельной таблицы категорий." },
    ],
  },

  "go-foundations-database-basics-2": {
    summary: "Создаём таблицу и разбираемся, зачем нужны первичный ключ и ограничение NOT NULL.",
    objectives: [
      "Написать CREATE TABLE с несколькими столбцами",
      "Понять роль PRIMARY KEY",
      "Использовать NOT NULL там, где поле обязательно",
    ],
    blocks: [
      { type: "heading", level: 2, text: "CREATE TABLE — чертёж будущих данных" },
      { type: "code", language: "sql", code: "CREATE TABLE expenses (\n    id SERIAL PRIMARY KEY,\n    title TEXT NOT NULL,\n    amount NUMERIC NOT NULL,\n    category_id INTEGER,\n    created_at TIMESTAMP DEFAULT now()\n);" },
      { type: "paragraph", text: "**PRIMARY KEY** — уникальный идентификатор строки, по которому её однозначно можно найти; `SERIAL` значит, что база данных будет присваивать id автоматически по возрастанию, вручную его указывать не нужно. **NOT NULL** — ограничение: база физически не даст сохранить строку без значения в этом столбце, даже если об этом забудет разработчик на стороне Go." },
      { type: "callout", tone: "tip", title: "Ограничения — это страховка на уровне данных", text: "Даже если в коде Go случайно пропустить проверку суммы расхода, NOT NULL на amount не даст сохранить некорректную запись. Это защита, которая работает независимо от того, кто и как пишет запросы к базе." },
    ],
  },

  "go-foundations-database-basics-3": {
    summary: "Четыре команды, которые составляют почти весь повседневный SQL: SELECT, INSERT, UPDATE, DELETE.",
    objectives: [
      "Выбрать данные из таблицы с условием через SELECT и WHERE",
      "Добавить новую строку через INSERT",
      "Изменить и удалить существующие записи",
    ],
    blocks: [
      { type: "heading", level: 2, text: "CRUD на языке SQL" },
      { type: "code", language: "sql", code: "-- добавить новую запись\nINSERT INTO expenses (title, amount, category_id)\nVALUES ('Обед в кафе', 850, 2);\n\n-- выбрать расходы дороже 500\nSELECT title, amount FROM expenses WHERE amount > 500;\n\n-- изменить сумму у конкретной записи\nUPDATE expenses SET amount = 900 WHERE id = 4;\n\n-- удалить запись\nDELETE FROM expenses WHERE id = 4;" },
      { type: "paragraph", text: "Эти четыре команды покрывают большую часть повседневной работы с данными — недаром аббревиатура CRUD (Create, Read, Update, Delete) стала общим термином независимо от языка и базы данных. `WHERE` — условие, без которого UPDATE и DELETE применятся ко **всем** строкам таблицы, поэтому его пропуск — одна из самых опасных ошибок в SQL." },
      { type: "callout", tone: "warning", title: "DELETE без WHERE удаляет вообще всё", text: "DELETE FROM expenses; (без WHERE) сотрёт все строки таблицы. Прежде чем выполнять UPDATE или DELETE в реальной базе, полезно сначала прогнать тот же WHERE через SELECT и убедиться, что выбраны именно нужные строки." },
    ],
  },

  "go-foundations-database-basics-4": {
    summary: "Миграции — способ версионировать структуру базы данных так же, как Git версионирует код.",
    objectives: [
      "Объяснить, зачем нужны файлы миграций вместо ручных ALTER TABLE",
      "Понять идею up и down миграции",
      "Связать миграции с командной работой над одной базой",
    ],
    blocks: [
      { type: "heading", level: 2, text: "Миграция — это commit для структуры базы данных" },
      { type: "paragraph", text: "Если менять структуру таблиц вручную прямо в базе, то у каждого разработчика команды со временем окажется чуть-чуть разная база — а тем более на сервере. **Миграция** решает эту проблему так же, как Git решает проблему рассинхронизации кода: изменение структуры базы описывается файлом, файл коммитится в репозиторий, и все — включая продакшен-сервер — применяют его в одном и том же порядке." },
      { type: "code", language: "sql", code: "-- 0002_add_category_to_expenses.up.sql\nALTER TABLE expenses ADD COLUMN category_id INTEGER;\n\n-- 0002_add_category_to_expenses.down.sql\nALTER TABLE expenses DROP COLUMN category_id;" },
      { type: "paragraph", text: "Файл `.up.sql` применяет изменение вперёд, `.down.sql` — умеет откатить его назад, если что-то пошло не так. Порядковый номер в начале имени файла (`0002_`) гарантирует, что миграции применяются в правильной последовательности на любой машине." },
    ],
  },

  "go-foundations-database-basics-5": {
    summary: "Подключаем Go-приложение к PostgreSQL через database/sql и первый живой запрос из кода.",
    objectives: [
      "Открыть соединение с базой данных через sql.Open",
      "Проверить соединение через Ping",
      "Выполнить первый запрос и прочитать результат в Go-структуру",
    ],
    blocks: [
      { type: "heading", level: 2, text: "От SQL-запроса в консоли до запроса из Go-кода" },
      { type: "code", language: "go", code: "import (\n\t\"database/sql\"\n\t_ \"github.com/lib/pq\" // драйвер PostgreSQL, подключается ради побочного эффекта\n)\n\ndb, err := sql.Open(\"postgres\", os.Getenv(\"DATABASE_URL\"))\nif err != nil {\n\tlog.Fatal(err)\n}\ndefer db.Close()\n\nif err := db.Ping(); err != nil {\n\tlog.Fatal(\"база данных недоступна:\", err)\n}" },
      { type: "paragraph", text: "`sql.Open` не устанавливает соединение сразу — только готовит объект для работы, поэтому важно вызвать `db.Ping()`, чтобы явно проверить, что база отвечает, до того как приложение начнёт полагаться на неё. Подчёркивание перед импортом драйвера (`_ \"github.com/lib/pq\"`) — идиома Go: пакет импортируется только ради его побочного эффекта регистрации драйвера, сами функции из него напрямую не вызываются." },
      { type: "code", language: "go", code: "rows, err := db.Query(\"SELECT title, amount FROM expenses WHERE amount > $1\", 500)\nif err != nil {\n\tlog.Fatal(err)\n}\ndefer rows.Close()\n\nfor rows.Next() {\n\tvar title string\n\tvar amount float64\n\trows.Scan(&title, &amount)\n\tfmt.Println(title, amount)\n}" },
      { type: "callout", tone: "warning", title: "$1, а не подстановка строк", text: "Значение 500 передаётся отдельным параметром ($1), а не вклеивается в текст запроса напрямую. Это защищает от SQL-инъекций — темы, к которой мы ещё вернёмся в проекте Expense Tracker." },
      { type: "quiz", question: "Зачем в примере используется db.Query(\"...WHERE amount > $1\", 500), а не просто вставить 500 в текст строки запроса?", options: ["Так короче писать код", "*Параметризация защищает от SQL-инъекций — опасной уязвимости при склейке пользовательских данных в запрос", "$1 нужен только для запросов с несколькими условиями"], explanation: "Прямая подстановка значений в текст SQL-запроса — классическая уязвимость; параметризованные запросы передают данные отдельно от текста запроса и исключают этот риск." },
    ],
  },

};
